using UnityEngine;
using System.Collections;

public class powerup_dash : MonoBehaviour { 
	//public float player_speed; 
	// Use this for initialization
	void Start () {
		//player_speed = GameObject.Find ("player").GetComponent<controller>(player_speed); 
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	//void OnTriggerEnter (Collider rigidbody) {
			//Destroy (this.gameObject); 
			//controller.player_speed += 2; 
	//}
}
